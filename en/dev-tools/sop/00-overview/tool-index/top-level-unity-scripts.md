> 🌐 English translation · [中文原文](../../../../../开发工具/SOP/00_总表/工具索引/顶层-Unity脚本.md)

> ← [Tool index](../tool-index.md) <!-- tools_index.py auto-generated: whole page overwritten, do not edit by hand -->

# Tool index · Top level - Unity scripts (20 files)

Generated by `python3 开发工具/通用工具/tools_index.py --write` from the "Purpose / Reusability" fields in each script's file header; the whole page is overwritten, so to change content edit the script file headers. Paths are relative to `开发工具/`.

### Tools (top level)

| Path | Purpose | Reusability |
|---|---|---|
| `通用工具/AvatarDump.cs` | Collection of read-only avatar exports: BlendShape names and weights, renderer bounding boxes (find the culprit inflating the AABB), renderer material slots and main textures | ★★★ Auto-detects the avatar, works as-is on another order |
| `通用工具/AvatarPortrait.cs` | Brings its own camera and lighting to render character showcase shots (front / three-quarter / back / face) + spec JSON | ★★★ Works as-is on another order |
| `通用工具/BakeTest.cs` | Without uploading or entering Play, runs an NDMF manual bake directly to reproduce build-time errors | ★★★ Auto-detects the avatar, works as-is on another order |
| `通用工具/BuildBlockerScan.cs` | Finds configuration problems that would abort an NDMF build, and idle PhysBones that can be safely reclaimed | ★★★ Auto-detects the avatar, works as-is on another order |
| `通用工具/FindDupComponents.cs` | Finds duplicate components on the avatar by full type name, and deletes the extras | ★★★ Change TypeNames to check other components |
| `通用工具/FitAAO.cs` | Attaches TraceAndOptimize via reflection and dumps its private serialized settings. The AAO runtime assembly doesn't have Auto Referenced enabled, so a direct using won't compile | ★★★ Works as-is on another order |
| `通用工具/FixAPSBounds.cs` | Restores the localBounds that AvatarPoseSystem grab handles deliberately inflate to 9.6 m back to the mesh's real bounding box, fixing AABB overflow. | ★★ Change the target object name at the top to use it for other plugins that inflate localBounds |
| `通用工具/FixAaoMergePhysBone.cs` | Health-checks AAO MergePhysBone on the avatar and deletes those with invalid configuration that would abort the build | ★★★ Auto-detects the avatar, works as-is on another order |
| `通用工具/FixMipStreaming.cs` | Batch-enables Mip Streaming (required by VRChat; the NDMF build errors without it) | ★★★ Works as-is on another order |
| `通用工具/FixNullMaterials.cs` | Fills empty material slots (rendered magenta) by retrieving materials from the **prefab source** | ★★★ Works as-is on another order |
| `通用工具/McpAutoStart.cs` | Makes the MCP bridge recover automatically after a Unity cold start, sparing a manual Transport click every time | ★★★ Usable in any project with MCP for Unity installed |
| `通用工具/ParamAudit.cs` | Audits expression parameters — how many bits each uses, whether it is referenced by the menu, whether it is used by an animator layer — to find deletable ones | ★★★ Auto-detects the avatar, works as-is on another order |
| `通用工具/PerfReport.cs` | Produces a performance report via the VRChat SDK AvatarPerformance API; includes polygon distribution grouped by top level. The report is also written to disk (the Console truncates multi-line logs) | ★★★ Works as-is on another order |
| `通用工具/PhysBoneTools.cs` | PhysBone tool collection: audit (inventory/Dump), find and delete duplicates, attach AAO MergePhysBone (same-parent group / exact naming), edit-time hard merge, squeeze under the 256 limit | ★★★ Auto-detects the avatar, works as-is on another order (ShedPhysBones requires changing the target table at the top) |
| `通用工具/PlayTest.cs` | Animator layer logic regression test. It records two dead ends: animator.SetFloat can't reach VRC's FX parameters (those are attached by the PlayableGraph), and animator.playableGraph returns an invalid graph under GestureManager. What works is putting the controll… | ★★★ Works as-is on another order |
| `通用工具/SceneViewShots.cs` | Positions the Scene View camera by Head/Foot bone world coordinates, for A/B comparison screenshots reproducible across changes | ★★★ Works as-is on another order |
| `通用工具/State70.cs` | 70 · Shared evaluator that "poses the avatar into the delivered state from a set of parameters"; acceptance renders / foot-shape measurement / combination traversal all share the same semantics. | ★★ Evaluator is generic, parameter table filled per base avatar |
| `通用工具/TexOptimize.cs` | Historical reference, only compression format handling kept; texture tiering has been superseded by tex_tier_plan.py / tex_tier_apply.py. | ★★★ Works as-is on another order |
| `通用工具/TraceParam.cs` | Traces what a parameter actually drives — parameter → animator layer → clip → curve target, and checks whether the target still exists | ★★ Usable after changing the parameter name table at the top |
| `通用工具/TrimDeadParams.cs` | Removes dead parameters that came with the base avatar but no longer have corresponding meshes, freeing sync bits | ★★ Usable after changing the parameter name table at the top |
