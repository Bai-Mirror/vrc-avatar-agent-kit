> 🌐 English translation · [中文原文](../../../../../开发工具/SOP/00_总表/工具索引/顶层-Python.md)

> ← [Tool index](../tool-index.md) <!-- tools_index.py auto-generated: whole page overwritten, do not edit by hand -->

# Tool index · Top level - Python (26 files)

Generated by `python3 开发工具/通用工具/tools_index.py --write` from the "Purpose / Reusability" fields in each script's file header; the whole page is overwritten, so to change content edit the script file headers. Paths are relative to `开发工具/`.

### Tools (top level)

| Path | Purpose | Reusability |
|---|---|---|
| `通用工具/agy_panel.py` | Adversarial review. Two usage tiers — | ★★★ Works as-is on another order |
| `通用工具/bl_keydump.py` | Dumps the full real-name table of all blend shapes in a .blend (key name + default value + slider range + affected vertex count); run it before starting on a base avatar whose vendor doesn't publish key names. | ★★★ Works as-is on another order |
| `通用工具/bl_keymeasure.py` | Opens a .blend and outputs, for the given blend shapes, displacement magnitude, mean direction, and centroid of the affected region. | ★★★ Works as-is on another order |
| `通用工具/bl_keyprobe.py` | Blender headless reads blend shape vertex displacements directly to determine each key's real direction of effect; no rendering, no model feeding. | ★★★ Works as-is on another order |
| `通用工具/bl_tilt.py` | Precisely measures "is the outer eye corner raised or lowered relative to the inner corner": first splits left/right eyes by x, then compares Δz of the outermost 10% and innermost 10% clusters of each eye. | ★★ The eye-region height band threshold must be tuned per base avatar |
| `通用工具/check_links.py` | Checks whether the target files of all relative .md links in a markdown tree (default `开发工具/SOP`) exist. | ★★★ Works as-is on another order |
| `通用工具/cjk_font.py` | Font loader for writing Chinese in annotated images, avoiding ImageFont.load_default() which has no CJK glyphs and renders boxes. | ★★★ Works as-is on another order |
| `通用工具/comfy_redraw.py` | Sends showcase shots into the local ComfyUI for derivative redraws (img2img + ControlNet lineart) | ★★★ Works as-is on another order |
| `通用工具/distill_collect.py` | Input collector for the two-tier "wrap-up distillation": gathers the "to-distill" tags after a given point in time (from each work log), the "lessons that should go into the SOP" sections from DSH replies (dispatch ledger), | ★★★ Works as-is on another order |
| `通用工具/ds_vision.py` | DeepSeek vision model client, used as the fourth review model for agy_panel | ★★★ Works as-is on another order |
| `通用工具/dsh_balance.py` | Read-only query of the balance on DSH's actual credentials; does not show keys, does not consume model quota. | (unmarked) |
| `通用工具/dump_vrc_menu.py` | Without opening Unity, reconstructs the VRCExpressionsMenu tree + VRCExpressionParameters directly from .asset YAML | ★★★ Works as-is on another order |
| `通用工具/eye_composite.py` | Offline eye texture compositing: alpha-over the eye color layer onto the base avatar base map to produce a complete _MainTex; heterochromia split by UV island. | ★★ Input directory must change for a different asset pack |
| `通用工具/guid_uniqueness_audit.py` | Project-wide GUID uniqueness audit. **One GUID may map to only one path** — | ★★★ Works as-is on another order |
| `通用工具/img_tools.py` | Collection of small image tools (subcommands): sheet contact sheet / sheet-by-tag pick images by tag into a sheet / diff-crop crop difference hotspots / overlay alpha-over composite of an overlay layer / autocrop crop tight by pixels | ★★★ Works as-is on another order |
| `通用工具/lens_readability.py` | Measures "whether the meteor/eyeball effect is readable": outputs background-corrected energy and ΔE diagnostics by viewing distance, giving a reproducible criterion. | ★★★ Works as-is on another order |
| `通用工具/log_step.py` | Appends a work log entry to a project/long-running task (timestamp only from the system clock), maintains the "status header" at the top of the file, optional one-step commit; reads back after writing and does not commit if the readback doesn't match. | ★★★ Works as-is on another order |
| `通用工具/pack_usage.py` | Produces an asset usage table by GUID: for each item in the asset pack, whether it was imported, whether it's used, and how complete it is. | ★★★ Works as-is on another order |
| `通用工具/project_fingerprint.py` | **Without opening Unity or installing third-party packages**, parses a Unity project purely from files (YAML and .meta of Assets + Packages), | ★★★ Works as-is on another order |
| `通用工具/shapekey_audit.py` | Full blend shape impact check after face sculpting bake: animation peak plus baked amount > 100 is judged as double-application deformation. | ★★★ Works as-is on another order |
| `通用工具/tex_tier_apply.py` | **Applies the tiering plan** produced by `tex_tier_plan.py` **to .meta**, while also writing a record that allows exact rollback. | ★★★ Works as-is on another order |
| `通用工具/tex_tier_plan.py` | Computes the texture tiering plan **without opening Unity** — scene → material → texture → .meta import settings, the whole chain in plain text. | ★★★ Works as-is on another order |
| `通用工具/tools_index.py` | Scans scripts under `开发工具/通用工具/`, extracts the "Purpose / Reusability" of the five file-header fields into markdown tables grouped by directory, writes them paginated into `开发工具/SOP/00_总表/工具索引/`, and writes navigation into the auto section of `工具索引.md`; `--check` lists files missing "Purpose". | ★★★ Works as-is on another order |
| `通用工具/unpack_unitypackage.py` | Unpacks an asset pack (zip → .unitypackage) into a Unity project's Assets/, without going through Unity and without changing GUIDs | ★★★ Works as-is on another order |
| `通用工具/vpm_baseline_check.py` | Compares "versions in the project's locked section" against "the latest stable version available in the VCC cache", | ★★★ Works as-is on another order |
| `通用工具/ws_git.py` | The sole commit entry point for workspace git (mandatory rule 6: write a work log entry and commit for every step). | ★★★ Works as-is on another order |
