> 🌐 English translation · [中文原文](../../../../../开发工具/SOP/00_总表/工具索引/顶层-Node与Shell.md)

> ← [Tool index](../tool-index.md) <!-- tools_index.py auto-generated: whole page overwritten, do not edit by hand -->

# Tool index · Top level - Node and Shell (10 files)

Generated by `python3 开发工具/通用工具/tools_index.py --write` from the "Purpose / Reusability" fields in each script's file header; the whole page is overwritten, so to change content edit the script file headers. Paths are relative to `开发工具/`.

### Tools (top level)

| Path | Purpose | Reusability |
|---|---|---|
| `通用工具/agy_mcp_server.js` | Wraps agy (Antigravity) multi-model review as an **MCP server**, | ★★★ Works as-is on another order |
| `通用工具/baseline_recheck.sh` | "Baseline recheck" after a crash / context compaction / new session: memory, Unity and DSH processes, locks, each project's uncommitted changes and status header, in-progress to-dos. Read-only, changes nothing. | ★★★ Works as-is on another order |
| `通用工具/dsh_session.js` | Reads DeepSeek Harness session logs back into plain text, for **dispatch retrospectives and performance evaluation**. | ★★★ Works as-is on another order |
| `通用工具/dsh_snapshot.js` | Execution state of DSH before/after snapshots, persistence, and comparison that preserves line order/duplicate lines; does not call a model. | (unmarked) |
| `通用工具/dsh_snapshot.test.js` | Regression for snapshot failures and the dispatch gate; uses only temp directories and a local fake executor, never calls a DSH model. | (unmarked) |
| `通用工具/dsh_task.js` | Dispatches one job to DeepSeek Harness, and **afterwards verifies from the session log what it actually did**. | ★★★ Works as-is on another order |
| `通用工具/dsh_usage.js` | Estimates DSH tokens and cost from the ~/.dsh/sessions session logs (by model, by time slot, cache hits); for reconciliation the DeepSeek console is authoritative. | (unmarked) |
| `通用工具/unity_csc.sh` | Compiles Unity assemblies offline using the project's own .rsp, without entering Unity or triggering a domain reload. | ★★★ Works as-is on another order |
| `通用工具/unity_recover.sh` | Recovery check and cleanup after a Unity crash / hang / OOM. By default it only reports (processes, log tail, locks, MCP status, project uncommitted changes); | ★★★ Works as-is on another order |
| `通用工具/unity_run.sh` | Unified Unity launcher. Both interactive and batch runs start here: always -force-vulkan; checks available memory before starting; refuses to start another if the project already has a live Unity; | ★★★ Works as-is on another order |
