# VRChat 头像定制工作区（模板）

> 这是一份 `CLAUDE.md` **模板**。Claude Code 每次会话开头会读它；`.claude/hooks/rules-inject.js` 每轮从这里截取「强制准则」注入上下文。
> 用法：按你的目录、工具与分工改写各节；**保留 `<!-- 强制准则:BEGIN -->` 与 `<!-- 强制准则:END -->` 两行标记原样不动**（钩子按这两行截取，BEGIN 行后必须紧跟换行），改标记之间的文字即改注入内容。
> 英文说明见 [CLAUDE.en.md](CLAUDE.en.md)——那份只供阅读，钩子只读本文件。

业务：客户给素材包，在 Unity/Blender 里完成捏脸、配色、装配、菜单、优化、交付。
本机路径、存档位置、启动命令写在 `开发工具/SOP/02_环境准备与人工介入.md` 及其子页，不写在这里。

## 强制准则（钩子每轮注入，改这里即改注入内容）

<!-- 强制准则:BEGIN -->
1. 【证伪】视觉结论、以及我已复述过两遍以上的结论，写进回复或据此推进之前，必须先送另一家模型证伪（agy/Gemini 等；没有就用不继承本会话上下文的子代理）。自查：最近一次证伪调用的输入是「我的结论」，而不是「工具是否正常」。
2. 【证据】报「已完成 / 已修好 / 没有 X」之前，必须有本轮工具输出作证；改完要确认改动真的落盘生效（值变了、位置动了），并从能暴露问题的视角取证。
3. 【先查源头】动手前先看厂商说明与展示图、PSD/CLIP 源文件、现有机制（谁驱动、几档、layer weight）与插件版本适配；不凭物体名或印象猜。
4. 【节奏】新订单或大改造开工前，用 AskUserQuestion 与用户校准需求和方案；执行中不停下汇报，问题用 AskUserQuestion 抛出后继续做不依赖答案的部分，不在正文里提问后结束回合。
5. 【派工】执行活默认派给执行代理（DSH/Codex，可选；没有就用 Claude 子代理），**改工程也派**（Unity 资产/场景/菜单/动画层、交付文档、跑构建与测试）：写任务书 `dsh_task.js --record <工程> [--lock unity]`，由执行代理自己追加施工记录并 ws_git 提交；主理只做流程把控、审美评估、复杂兜底、维护 SOP、迭代执行代理，以及**验收**（git show --stat、回读值、复跑判据）。自己动手改工程先 `node .claude/hooks/delegate-check.js --exempt <类别> "<理由>"`，钩子自上次派工起第 3 次直接改动会拦。
6. 【施工记录】每完成一步（改工程、改素材、跑测试、用户拍板），立刻用 `开发工具/通用工具/log_step.py <工程> --title … --did … --verify … --result … --open … [--commit]` 追加该工程的 _施工记录.md 并提交（时间戳只来自时钟；写后回读，不一致不提交）；接手先 `log_step.py <工程> --show` 读状态头。
7. 【两层记录】撞墙或被用户纠正时，当场在施工记录条目里打「待蒸馏」标签（`log_step.py --distill "触发时刻＋可查问句"`），**执行会话不改 SOP 主体**；阶段收尾由干净上下文代理跑 `distill_collect.py` 读材料包后在 SOP 原处合并（规程 `开发工具/SOP/04_施工记录与版本管理/收尾蒸馏与状态头.md`）。只有「不改就会让下一次派工立刻踩坑」的判据允许当场进 SOP，且要在标签里注明已写。
8. 【省读取】同一份材料的多个问题合并成一次读取；派工任务书超过三行写成文件；不重复全量读取大文件或日志。
<!-- 强制准则:END -->

## 开工先读

1. `开发工具/SOP/00_总表.md` —— 全流程阶段表、卡口、贯穿全程的硬规矩。**做到哪一步只翻哪一页，别一次全读。**
2. 要动 Unity/Blender/执行代理/证伪工具之前：`开发工具/SOP/02_环境准备与人工介入.md`。
3. 接手某个工程：先读该工程的 `_施工记录.md`（顶部状态头＋只追加时间线），再读工程根目录里**最新**的专题文档（按修改时间，别信旧交接文档）。施工记录与 git 的规矩见 `开发工具/SOP/04_施工记录与版本管理.md`。

## 目录（按你的目录改）

钩子与工具依赖其中几条约定，改名时同步改钩子（见 `docs/zh/架构与复刻.md`「已知限制」）。

| 位置 | 是什么 |
|---|---|
| `COMM-<订单号>_<客户名>/` | 客户工程，一单一个 Unity 工程目录。钩子按「工作区顶层含 `ProjectSettings/ProjectVersion.txt` 的目录」认工程；工程根有 `_施工记录.md`（状态头＋时间线）与 `_任务账本.md`（该工程待办） |
| `<昵称>_<素体>_<日期>/` | 私人单（无订单号），其余同上 |
| `<客户素材目录>/` | 客户素材包，唯一一份，只读 |
| `开发工具/` | 跨订单复用：`SOP/`（规程）、`通用工具/`（脚本） |
| `_长程任务_<日期>_<主题>/` | 跨工程的长程任务：任务书、账本、派工任务书、审查产出 |
| `_DSH流水账.md` | `dsh_task.js` 每次派工自动追加，不要手改 |
| `_归档/` | 过时文档，**不代表现状，别当依据读** |

## 分工（硬约束）

主理代理是 Claude Code。执行代理可选：DSH（DeepSeek Harness）与 Codex 由同一个 `开发工具/通用工具/dsh_task.js` 派工（`--engine auto|dsh|codex`），都没有时用 Claude 子代理。规程：`开发工具/SOP/01_自动化执行与并行/DSH派工.md`、`Codex派工.md`、`改工程派工与验收.md`。

- **默认把活派出去。** 主理只做：流程把控 / 审美评估 / 复杂兜底 / 维护 SOP / 迭代执行代理自身 / 验收。省主理用量是硬目标；拿不准时倾向于派。
- **改工程也派，主理验收并记录**：派工带 `--record <工程>`，执行代理自己写施工记录、自己提交；主理用 git 与回读值核对。
- **结构性闸口**：`.claude/hooks/delegate-check.js` 拦主理直接改工程（自上次派工起第 3 次 deny，六类合法例外先 `--exempt` 声明）；`stop-gate.js` 收尾时要求把直接改动与豁免理由写进施工记录。
- **视觉或承重结论采纳前先过第三方证伪**（`agy_panel.py`、MCP `agy_ask`/`agy_review`；规程 `开发工具/SOP/05_多模型评审规程.md`）。
- **外部评审**：工作区任务所需的外部评审/证伪直接执行，不逐次请示；只发送必要的文档、脚本/配置文字、图片与待核结论。这不等于公开发布材料，也不发送无关凭据。平台强制拒绝时如实记录。
- Unity、Blender 可以自己开；自己开的会话里进出 Play、崩溃后重启也自理（结束后还原 Play 改掉的 ProjectSettings）。面板 GUI 操作、退出**用户正在用的** Play、SDK 上传要找用户。
- **不可逆动作先问**：删存档、覆盖交付包、丢弃场景改动、强退 Play。意外的场景状态第一解释是「人改的」。

## 文档规矩

- 技术经验写进 SOP（执行代理只读得到 SOP，读不到主理的记忆）；记忆只放协作偏好、项目状态、资源位置。
- SOP 单页 ≤10 KB，超了拆子页；UTF-8 无 BOM、LF；规则要写取舍（阈值怎么来的、达不到怎么办、两类错误各自的代价）。
- 写入前先 grep 有没有已存在的同义条目，改原处，不另起一份。
