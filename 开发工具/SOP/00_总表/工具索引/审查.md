> ← [工具索引](../工具索引.md) <!-- tools_index.py 自动生成：整页覆盖，勿手改 -->

# 工具索引 · 审查（4 个文件）

由 `python3 开发工具/通用工具/tools_index.py --write` 从各脚本文件头的「用途 / 可复用性」生成，整页覆盖；要改内容请改脚本文件头。路径相对 `开发工具/`。

### 通用工具/审查

| 路径 | 用途 | 可复用性 |
|---|---|---|
| `通用工具/审查/dual_root_diff.py` | 双头像根组件差异普查（离线只读 Unity YAML），找面捕克隆根与原生根被悄悄改得不一致的地方。 | ★★ 按工程目录跑即可用 |
| `通用工具/审查/menu_expect_check.py` | W5「编译态菜单断言」：期望（第二视角，交付说明写出）与实测（构建后 T1 扫描）机械比对， | ★★★ 期望 JSON 格式固定；参数名/取值沿用 T4 params.json |
| `通用工具/审查/menu_expect_check_selftest.py` | 离线验证菜单行为与覆盖判据，包含必须拒绝的假放行反例。 | （未标） |
| `通用工具/审查/t5_shapekey_matrix.py` | T5「形态键依赖矩阵」：用 slots/sweep 生成 T1 请求，再 analyze 出「哪个开关动哪个键、谁该收谁没收」。 | ★★ 声明/参数结构变了要跟着改 |
