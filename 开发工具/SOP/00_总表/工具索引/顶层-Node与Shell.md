> ← [工具索引](../工具索引.md) <!-- tools_index.py 自动生成：整页覆盖，勿手改 -->

# 工具索引 · 顶层-Node与Shell（10 个文件）

由 `python3 开发工具/通用工具/tools_index.py --write` 从各脚本文件头的「用途 / 可复用性」生成，整页覆盖；要改内容请改脚本文件头。路径相对 `开发工具/`。

### 通用工具（顶层）

| 路径 | 用途 | 可复用性 |
|---|---|---|
| `通用工具/agy_mcp_server.js` | 把 agy（Antigravity）的多模型评审包成 **MCP 服务器**， | ★★★ 换个单子直接能用 |
| `通用工具/baseline_recheck.sh` | 崩溃 / 上下文压缩 / 新会话之后的「基线重核」：内存、Unity 与 DSH 进程、锁、各工程未提交改动与状态头、进行中待办。只读，不改任何东西。 | ★★★ 换个单子直接能用 |
| `通用工具/dsh_session.js` | 把 DeepSeek Harness 的会话日志读回明文，用于**派工复盘与性能评估**。 | ★★★ 换个单子直接能用 |
| `通用工具/dsh_snapshot.js` | DSH 前后快照的执行状态、落盘与保留行序/重复行的比较；不调用模型。 | （未标） |
| `通用工具/dsh_snapshot.test.js` | 快照失败与派工闸口回归；只用临时目录和本地伪执行器，绝不调用 DSH 模型。 | （未标） |
| `通用工具/dsh_task.js` | 向 DeepSeek Harness 派一次工，并**在事后从会话日志里核验它到底做了什么**。 | ★★★ 换个单子直接能用 |
| `通用工具/dsh_usage.js` | 按 ~/.dsh/sessions 会话日志估算 DSH 的 token 与费用（按模型、按时段、缓存命中），对账以 DeepSeek 后台为准。 | （未标） |
| `通用工具/unity_csc.sh` | 用工程自己的 .rsp 离线编译 Unity 程序集，不进 Unity、不触发域重载。 | ★★★ 换个单子直接能用 |
| `通用工具/unity_recover.sh` | Unity 崩溃 / 卡死 / OOM 后的恢复检查与清理。缺省只报告（进程、日志尾、锁、MCP 状态、工程未提交改动）； | ★★★ 换个单子直接能用 |
| `通用工具/unity_run.sh` | 统一的 Unity 启动器。交互或批处理都从这里起：一律 -force-vulkan；起前查可用内存；工程已有活的 Unity 就拒绝另起； | ★★★ 换个单子直接能用 |
