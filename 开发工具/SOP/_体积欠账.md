# SOP 单页体积欠账

> 规程自定上限 **10 KB**（`00_总表` §单页体积）。超了就拆子页：下沉「机制解释 / 踩坑复盘 / 推导过程」，**步骤表与判据永远留主页**。
> **拆的时机是同一页出现第三个互不相关的主题**，不是到了某个字节数；判据完整 > 单页体积 > 篇数少。
> 2026-09-08 以前的逐轮记录已归档：`_归档/2026-09-18_文档精简/开发工具/_体积欠账_至20260908.md`。

## 怎么量（每次给某页加完内容后跑）

```bash
cd ~/vrc-processing/开发工具 && python3 -c "
import glob,os
a=glob.glob('SOP/**/*.md',recursive=True)
o=[(os.path.getsize(p),p) for p in a if os.path.getsize(p)>10240]
print('共 %d 页，超限 %d 页'%(len(a),len(o)))
for n,p in sorted(o,reverse=True): print('  %7d B  %s'%(n,p))
"
```

改页前先 `wc -c` 看余量；插入或搬移章节后 `grep '^#' 页面` 查重复的一级标题与丢失的标题，搬走的内容在原位留指针（2026-09-19 BM 把 70 的「常见坑」标题覆盖、30 的验收表搬走没留指针）。

拆前快照现拍、超限只数本轮页：见 [收尾蒸馏](04_施工记录与版本管理/收尾蒸馏与状态头.md) 规则层 2。

一律量**磁盘字节**（`os.path.getsize`），别量内存字符串；写文件用 `newline='\n'` 或二进制写，保持 LF。收工前顺手查 CRLF（`grep -rlI $'\r' SOP`）。

## 现状（2026-09-19，101 页 / 541 KB，超限 2 页；BS 新增 `70_回归测试/常见坑`、`70_回归测试/视角与动态姿势` 两子页，均为给 70 主页腾字节）

| 字节 | 页 | 处置 |
|---:|---|---|
| 15,915 | `50_服装发型装配.md` | **豁免** |
| 10,250 | `70_回归测试/旧版_20260918_只当样本.md` | **豁免** |

### 两页豁免理由

- `50_服装发型装配.md` —— **随 D-03 重写时一并瘦身，不单独拆**。2026-09-18 作者判定其收缩键/穿模几节的方法不得直接复用，本就要随回归测试规程重写；任务 BH 明确不碰这一页及其子目录。
- `70_回归测试/旧版_20260918_只当样本.md` —— **只当样本、不维护**，冻结快照。09-25 移到 [_旧版/](_旧版/README.md)、修好 4 条断链、页首压缩后 10,216 B，已不超限；有效结论已提到现行页。

## 2026-09-19 这轮拆了 16 页（D-29～D-41）

| 页 | 拆前 | 拆后 | 新子页（同名字目录下） |
|---|---:|---:|---|
| `00_总表.md` | 12,110 | 10,037 | 两条跨阶段提醒（09-25 已并回总表，子页删除）/ 问题定位索引 / 人工卡口与素材来源 |
| `03_装配与冲突通则.md` | 10,462 | 8,892 | 装饰与变体的判据 |
| `05_多模型评审规程.md` | 10,507 | 9,806 | 编成取舍与单模型快评（09-25 已并回主页，子页删除） |
| `20_素材清点导入.md` | 10,307 | 9,213 | GUID 撞车与修法 |
| `30_捏脸.md` | 12,345 | 10,111 | 路线定案 / 硬规矩的依据与例外 / 30 与 50 的排期约束 / Blender 预览锯齿 |
| `40_贴图配色/素体贴图与改色手法.md` | 11,977 | 8,918 | lilToon 叠色层 / 竖向渐变 |
| `55_配饰装位.md` | 10,478 | 8,943 | 骨段长度判据两步 |
| `55_配饰装位/通用坑.md` | 10,590 | 8,995 | 镜像件缩放符号 / 蒙皮件 localBounds |
| `60_菜单与动画层.md` | 11,756 | 9,333 | 结构决策的实测样本 / 参数审计与语义冲突 / 不开 Unity 读菜单树 / 结构惯例 |
| `60_菜单与动画层/属性归属与写法.md` | 11,830 | 9,800 | 压制写法的代价与审计 / 同一位置多来源用轮盘 |
| `80_性能优化.md` | 10,953 | 9,448 | AAO 实际做了什么 |
| `80_性能优化/贴图与动骨.md` | 12,085 | 9,890 | 贴图分级判据与坑 |
| `90_交付打包.md` | 10,299 | 9,545 | 排除规则锚定工程根 |
| `问题定位/观测口径.md` | 14,446 | 6,562 | 判据本身可能是坏的 |
| `问题定位/观测口径/渲染与取景.md` | 12,800 | 9,707 | 出图管线三个坑 |
| `问题定位/素材与材质排查.md` | 10,308 | 9,070 | 厂商变体不一定两两不同 |

共新增 **27 个子页**，每个都 ≤10,240 B；主页也都 ≤10,240 B。做法：整段移动、不重写措辞，脚本 `split_page.py` 按行区间搬运并自动把子页里的相对链接按新位置重算。

核对（脚本在 `_长程任务_20260918/派工/tmp/bh/`）：
- **原文一行不少**：`check_lines.py` 逐页合并「主页＋其新子页」的非空行，与拆前存档 `before/` 比对，合计缺失 **0 行**（20 行仅因相对链接改写、1 行因反引号里的 `../` 路径改写，属第 4 条允许的路径改写，脚本单列）。
- **链接**：`check_links.py` 全 SOP 366 条相对 `.md` 链接，本次新增断链 **0**；拆前就断的 4 条（都在豁免的 `70 旧版` 里）列出未改。
- **编码**：UTF-8 无 BOM、LF；`grep -rlI $'\r' SOP` 为空。

## 下一步建议（没做，等拍板或等下次改到时顺手做）

| 页 | 建议 |
|---|---|
| `50_服装发型装配.md` | 随 D-03 重写时一并瘦身，别单独拆 |
| `30_捏脸.md`（10,111）、`00_总表.md`（10,037）、`80_性能优化/贴图与动骨.md`（9,890）、`60/属性归属与写法.md`（9,800） | 已贴近上限，下次往里加内容前先看是否出现第三个主题，先拆再加 |
| 各主页的「子页索引」表 | 本轮新子页只在原章节位置留了指针，索引表没逐条补（`30_捏脸` 已无字节余量）；下次改到时顺手补齐 |

## 2026-09-20 23:2x · 捏脸凹凸一轮写完后的体积
- 新增 `30_捏脸/捏脸凹凸的归因与修复.md`（5569 B）；`30_捏脸.md` 加索引后 10140 B（贴着上限，再加一条就要拆）。
- `问题定位/观测口径/判据本身可能是坏的.md` 加「遮挡类判据」后到 10266 B → **已拆出** `遮挡类判据.md`（1317 B），母页回到 9220 B。
- `02_环境准备与人工介入/B850_Linux环境.md` 加 pgrep/ERE 与 `read -t </dev/zero` 两条后到 11285 B →
  **已拆出** `B850_Linux环境/进程与等待.md`（2419 B，收纳 pkill/pgrep/等待三条），母页回到 9350 B。
- `40_贴图配色/素体贴图与改色手法.md` **本来就超**（11628 B），本轮只加了一行子页指针 → 11854 B。
  新子页 `素体贴图与改色手法/眼贴图包多半是整脸图集.md`（2647 B）。**该母页待拆，记在这里。**

## 2026-09-21 03:2x · 04 拆子页
- `04_施工记录与版本管理.md` 连加两条（heredoc 的 `%` 格式化坑、目录自身 `.meta` 漏提交）后到 10550 B →
  **已拆出** `04_施工记录与版本管理/落盘与提交的坑.md`（2029 B），母页回到 **9000 B**，原位留了指针。
- 全库复查：112 页，超限仍是那 2 页旧欠账（`40/素体贴图与改色手法.md` 11854、`70/旧版_20260918_只当样本.md` 10250）。
- 顺带澄清：`04` 第 24 行那条「2026-09-18 狐耳缩放」看着像混进来的施工记录，
  **实际是代码块里的模板示例**，不要误删。

## 2026-09-23 · W2 拆三页＋修 9 死链

| 页 | 拆前 | 拆后 | 新子页（同名字目录下） |
|---|---:|---:|---|
| `40_贴图配色/素体贴图与改色手法.md` | 11,854 | 9,692 | 拼瞳撞色与选色 |
| `80_性能优化.md` | 10,621 | 10,051 | 为什么不能读meta判格式 |
| `70_回归测试.md` | 10,307 | 9,101 | 必查清单实测案例 |

三个新子页分别 2,773 / 1,067 / 1,740 B，都 ≤10,240。做法仍是 `split_page.py` 整段移动
（spec 在 `_长程任务_20260918/派工/tmp/w2/`）：40 下沉页尾「两色拼瞳 / 撞色选色 / 明度补偿」三节，
80 下沉步骤 4 的「为什么不能读 `.meta` 判格式」机制解释，70 下沉「必查清单（七单实测里真出过的）」复盘表；
步骤表与判据都留在主页，原位留指针（`70_回归测试/旧版_20260918_只当样本.md` 豁免、未动）。

**修 9 条死链**（只改相对路径，文字不动；每条改前已 `ls` 目标存在）：

| 页 | 原路径 → 新路径 |
|---|---|
| `70_回归测试/常见坑/编辑器调用与重发.md` | `../02_环境准备与人工介入.md` → `../../…`；`../02_环境准备与人工介入/B850_Linux环境.md` → `../../…` |
| `60_菜单与动画层/VRCFury参数压缩.md` | `../../00_总表.md` → `../00_总表.md` |
| `50_服装发型装配/收缩键流程与两族.md` | `70_回归测试.md` ×2 → `../70_回归测试.md`；`问题定位/观测口径.md` → `../问题定位/观测口径.md` |
| `50_服装发型装配/兼容性判定.md` | `10_接单建档.md` → `../10_接单建档.md` |
| `50_服装发型装配/脚型与收缩键归属/两类键与写者.md` | `../问题定位/观测口径/判据的形状.md` → `../../…` |
| `05_多模型评审规程/证伪任务设计/小部件与像素复核.md` | `../01_自动化执行与并行/步骤模板与硬闸.md` → `../../…` |

**复核结果（三个数）**：
- 缺失行 **0**（`check_lines.py` 同一段逻辑，改用本次开拆前现盘快照 `tmp/w2/before_w2/`，比 17 页；仅 70 有 2 行因相对链接改写命中，属允许）。
- 新增断链 **0**（`check_links.py`：全 SOP 568 条相对 `.md`，断链 4 条全在豁免的 `70 旧版`）。
- 超限页 **1**：`70_回归测试/旧版_20260918_只当样本.md`（10,250，豁免）。⚠ 复核时 `00_总表/工具索引.md` 为 26,823 B，是并行任务 W1「工具索引脚本化」的进行中产物、在本轮不许碰清单内，**未动**，故不计入本次欠账。

> 注：09-19 的 `before/` 存档已被后续改动用到过期（直接重跑会报 00/30/60/渲染与取景 的旧行缺失），
> 所以本轮改用现盘快照另存 `tmp/w2/before_w2/`，再用 `check_lines.py` 同一段逻辑核对（包装脚本 `tmp/w2/check_lines_w2.py`）。

## 2026-09-23 · 豁免追加
- `00_总表/工具索引.md`：09-25 起 `tools_index.py --write` 把逐文件表分页写到 `00_总表/工具索引/<分页>.md`（各 ≤10 KB，超了自动切 -1/-2），主页只留手写总览＋分页导航（约 4.6 KB），**不再豁免**。
- 本轮 Claude 改 `00_总表.md`、`90_交付打包.md` 一度超限，已分别微缩、把 3b 来由下沉 `90_交付打包/编译态菜单断言.md`。
